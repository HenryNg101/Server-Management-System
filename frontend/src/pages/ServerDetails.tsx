import { useEffect, useState } from "react";
import { Link, useParams } from "react-router-dom";
import api from "../api/client";
import type { Server } from "../types/api";
import { Status } from "./Servers";

export default function ServerDetail() { const { id } = useParams(); const [server, setServer] = useState<Server | null>(null); const [error, setError] = useState(""); useEffect(() => { api.get<Server>(`/servers/${id}`).then(({ data }) => setServer(data)).catch(() => setError("This server could not be loaded.")); }, [id]); if (error) return <section className="panel error-panel">{error}</section>; if (!server) return <section className="panel loading-panel">Loading server details…</section>; return <><Link className="back-link" to="/servers">← Back to fleet</Link><section className="detail-hero"><div><span className="eyebrow">SERVER / {server.id}</span><h2>{server.name}</h2><p className="mono">{server.ipv4}</p></div><Status status={server.status} /></section><section className="detail-grid"><div className="panel"><div className="panel-heading"><div><h2>Endpoint details</h2><p>Current configuration and ownership.</p></div></div><dl className="detail-list"><div><dt>IPv4 address</dt><dd className="mono">{server.ipv4}</dd></div><div><dt>Server ID</dt><dd>{server.id}</dd></div><div><dt>Created</dt><dd>{new Date(server.created_at).toLocaleString()}</dd></div><div><dt>Last updated</dt><dd>{new Date(server.updated_at).toLocaleString()}</dd></div></dl></div><div className="panel health-panel"><span className="eyebrow">HEALTH</span><h2>{server.status === "UP" ? "Operating normally" : "No current health data"}</h2><p>The server service currently exposes aggregate health as <code>{server.status}</code>. Run an admin report for time-window uptime and resource statistics.</p><Link className="secondary-button inline-action" to="/reports">Open reporting →</Link></div></section></>; }
