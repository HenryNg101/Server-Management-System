# Using anchors and aliases to have a common config for multiple services (DRY principle)
x-logging: &default-logging
  driver: "json-file"
  options:
    max-size: "50m"
    max-file: "5"

x-common-dependency: &common-dependency
  postgres:
    condition: service_healthy
  redis:
    condition: service_healthy
  elasticsearch:
    condition: service_healthy
  kafka:
    condition: service_started
  postgres-migrate:
    condition: service_completed_successfully
  kafka-setup:
    condition: service_completed_successfully

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        CMD_PATH: ./cmd/api
    ports:
      - "${HOST}:8080:8080"
    restart: unless-stopped
    depends_on: *common-dependency
    env_file:
      - ${ENV_FILE}
    environment:
      - APP_ENV=docker
    logging: *default-logging
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.api.rule=PathPrefix(`/api`)"
      - "traefik.http.services.api.loadbalancer.server.port=8080"

  servers_checker:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        CMD_PATH: ./cmd/worker/servers_checker
    restart: unless-stopped
    depends_on: *common-dependency
    env_file:
      - ${ENV_FILE}
    environment:
      - APP_ENV=docker
    logging: *default-logging
  
  cron_scheduler:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        CMD_PATH: ./cmd/worker/cron_scheduler
    restart: unless-stopped
    depends_on: *common-dependency
    env_file:
      - ${ENV_FILE}
    environment:
      - APP_ENV=docker
    logging: *default-logging

  metrics_consumer:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        CMD_PATH: ./cmd/worker/metrics_consumer
    restart: unless-stopped
    depends_on: *common-dependency
    env_file:
      - ${ENV_FILE}
    environment:
      - APP_ENV=docker
    logging: *default-logging
  
  file_importer_consumer:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        CMD_PATH: ./cmd/worker/file_importer_consumer
    restart: unless-stopped
    depends_on: *common-dependency
    env_file:
      - ${ENV_FILE}
    environment:
      - APP_ENV=docker
    logging: *default-logging
  
  auth-service:
    build:
      context: .
      dockerfile: services/auth-service/Dockerfile
    ports:
      - "8081:8081"
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    env_file:
      - ${ENV_FILE}
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.auth.rule=PathPrefix(`/auth`)"
      - "traefik.http.services.auth.loadbalancer.server.port=8081"
      - "traefik.http.middlewares.auth-strip.stripprefix.prefixes=/auth"
      - "traefik.http.routers.auth.middlewares=auth-strip"
    environment:
      - APP_ENV=docker
      - PORT=8081
    logging: *default-logging

  traefik:
    image: traefik:latest
    command:
      - "--api.insecure=true"
      - "--providers.docker=true"
      - "--providers.docker.endpoint=unix:///var/run/docker.sock"
      - "--entrypoints.web.address=:80"
    ports:
      - "80:80"
      - "8088:8080" # Traefik dashboard
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock