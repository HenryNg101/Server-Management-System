services:
  postgres:
    ports:
      - "${HOST}:5432:5432"

  redis:
    ports:
      - "${HOST}:6379:6379"
      - "${HOST}:8001:8001"

  elasticsearch:
    ports:
      - "${HOST}:9200:9200"
  
  kibana:
    ports:
      - "${HOST}:5601:5601"